"use client";

import Link from "next/link";
import { useEffect, useRef } from "react";
import { initLogic } from "./logic";

const SUPABASE_URL = process.env.NEXT_PUBLIC_SUPABASE_URL || "";
const SUPABASE_ANON_KEY = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY || "";
const ROOM_TABLE = "co_ganh_rooms";
type GameApi = { cleanup: () => void; getState: () => unknown; loadState: (state: unknown) => void; newGame: (shouldNotify?: boolean) => void };

const markup = `
<main class="ganh-shell">
  <aside class="panel">
    <div class="brand-block"><div class="logo">◆</div><div><h1>Cờ Gánh đôi</h1><p>Chơi 1 máy hoặc tạo phòng online</p></div></div>
    <div class="card accent-card"><p class="label">Trạng thái</p><h2 id="status">Lượt Trắng</h2><p id="hint">Chọn quân để đi theo đường nối.</p></div>
    <div class="card online-card"><p class="label">Chơi online</p><p id="onlineStatus" class="online-status">Mặc định chơi 1 máy. Tạo phòng để mời người khác.</p><div class="room-actions"><button id="createRoomBtn" class="btn primary" type="button">Tạo phòng</button><button id="copyRoomBtn" class="btn" type="button" disabled>Copy link</button></div><div class="join-row"><input id="roomInput" class="room-input" placeholder="Nhập mã hoặc link phòng" /><button id="joinRoomBtn" class="small-btn" type="button">Vào</button></div></div>
    <div class="score-grid"><div class="card score"><p class="label">Quân Trắng</p><h2 id="whiteCount">8</h2></div><div class="card score"><p class="label">Quân Đen</p><h2 id="blackCount">8</h2></div></div>
    <button id="newBtn" class="btn primary" type="button">Ván mới</button><button id="undoBtn" class="btn" type="button">Hoàn tác</button><button id="flipBtn" class="btn" type="button">Lật bàn</button>
  </aside>
  <section class="board-wrap"><div class="board-frame"><div id="board" class="board" aria-label="Bàn cờ gánh 2 người"></div></div></section>
  <aside class="panel"><div class="card"><p class="label">Biên bản</p><ol id="moveList" class="moves"></ol></div><div class="card rules"><p class="label">Đã hỗ trợ</p><ul><li>Bàn 5x5, mỗi bên 8 quân</li><li>Đi 1 bước theo đường nối tới điểm trống</li><li>Gánh: vào giữa 2 quân đối phương để đổi màu</li><li>Vây: nhóm đối phương hết đường đi sẽ bị đổi màu</li><li>Đổi hết quân đối thủ là thắng</li><li>Phòng online cho 2 laptop</li></ul></div></aside>
</main><div id="modal" class="modal hidden"><div class="modal-card"><h2>Kết thúc ván</h2><p id="modalText"></p><button id="closeModal" class="btn primary" type="button">Đóng</button></div></div>`;

const styles = `
*{box-sizing:border-box}body{margin:0}.web_co_ganh-wrapper{min-height:100vh;font-family:Arial,Helvetica,sans-serif;color:#fff4df;background:radial-gradient(circle at 18% 8%,rgba(255,214,111,.2),transparent 32%),radial-gradient(circle at 82% 14%,rgba(92,159,67,.18),transparent 32%),linear-gradient(135deg,#1e1712,#241811 54%,#0b0705)}.back-link{position:absolute;top:16px;left:16px;z-index:20;border:1px solid rgba(255,255,255,.18);border-radius:8px;padding:9px 12px;background:rgba(255,255,255,.12);color:#fff;text-decoration:none;font-weight:900}.ganh-shell{min-height:100vh;display:grid;grid-template-columns:minmax(250px,320px) minmax(360px,650px) minmax(250px,320px);gap:18px;align-items:center;justify-content:center;padding:60px 22px 22px}.panel{border:1px solid rgba(255,255,255,.13);border-radius:8px;padding:20px;background:rgba(48,34,24,.88);box-shadow:0 24px 70px rgba(0,0,0,.3);backdrop-filter:blur(18px)}.brand-block{display:flex;gap:14px;align-items:center;margin-bottom:22px}.logo{width:58px;height:58px;border-radius:8px;display:grid;place-items:center;background:linear-gradient(145deg,#ffe2a0,#a96a31);color:#24140b;font-size:40px;font-weight:900}h1,h2,p{margin:0}.brand-block h1{font-size:30px;line-height:1.05}.brand-block p,.label,#hint,.rules li{color:#dbc5a1}.label{font-size:13px;text-transform:uppercase;letter-spacing:0;margin-bottom:8px;font-weight:900}.card{background:rgba(255,255,255,.07);border:1px solid rgba(255,255,255,.08);border-radius:8px;padding:16px;margin-bottom:14px}.accent-card{background:linear-gradient(145deg,rgba(92,159,67,.24),rgba(255,255,255,.07))}.card h2{font-size:26px;margin-bottom:8px}.score-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.score h2{font-size:42px}.btn{width:100%;border:0;border-radius:8px;padding:14px 16px;margin-top:10px;background:rgba(255,255,255,.12);color:white;font-size:16px;font-weight:900;cursor:pointer;transition:.16s}.btn:hover,.small-btn:hover{transform:translateY(-1px);filter:brightness(1.12)}.btn:disabled{cursor:not-allowed;opacity:.48;transform:none;filter:none}.primary{background:linear-gradient(180deg,#76b85d,#458d3f)}.online-status{margin-bottom:12px;color:#efd9b7;font-size:14px;line-height:1.45}.room-actions{display:grid;grid-template-columns:1fr 1fr;gap:10px}.room-actions .btn{margin-top:0}.join-row{display:grid;grid-template-columns:1fr auto;gap:10px;margin-top:10px}.room-input{width:100%;min-width:0;border:1px solid rgba(255,255,255,.14);border-radius:8px;padding:10px 11px;background:rgba(0,0,0,.18);color:#fff;outline:none}.small-btn{border:0;border-radius:8px;padding:8px 11px;background:rgba(255,255,255,.12);color:#fff;cursor:pointer;font-weight:900}.board-wrap{display:grid;place-items:center}.board-frame{padding:12px;border-radius:8px;background:rgba(11,7,5,.55);box-shadow:0 30px 90px rgba(0,0,0,.4)}.board{position:relative;width:min(88vw,620px);aspect-ratio:1/1;background:linear-gradient(145deg,#e4b06d,#d59a55);border:16px solid #4a2a13;border-radius:8px;box-shadow:inset 0 0 0 5px rgba(255,255,255,.16);overflow:hidden}.line{position:absolute;height:4px;background:#563116;transform-origin:left center;border-radius:8px;box-shadow:0 1px 0 rgba(255,255,255,.18)}.point{position:absolute;width:clamp(54px,8vw,74px);height:clamp(54px,8vw,74px);transform:translate(-50%,-50%);border:0;background:transparent;border-radius:50%;display:grid;place-items:center;cursor:pointer;z-index:5;padding:0}.point::before{content:"";position:absolute;width:12px;height:12px;border-radius:50%;background:#563116;opacity:.75}.point:hover{background:rgba(255,255,255,.14)}.point.selected{outline:6px solid rgba(255,222,89,.75);background:rgba(255,222,89,.18)}.point.to-move .piece{filter:drop-shadow(0 0 10px rgba(255,222,89,.58))}.point.legal::after{content:"";position:absolute;width:24px;height:24px;border-radius:50%;background:rgba(0,0,0,.3)}.point.last{background:rgba(255,255,255,.22)}.piece{width:78%;height:78%;border-radius:50%;z-index:8;box-shadow:0 7px 13px rgba(0,0,0,.45),inset 0 4px 8px rgba(255,255,255,.28);border:4px solid rgba(255,255,255,.28);pointer-events:none}.piece.w{background:radial-gradient(circle at 35% 25%,#fff,#f1dfbf 60%,#b98f58)}.piece.b{background:radial-gradient(circle at 35% 25%,#555,#171717 60%,#000)}.moves{max-height:430px;overflow:auto;margin:0;padding-left:22px;line-height:1.9}.rules ul{margin:0;padding-left:20px;line-height:1.7}.modal{position:fixed;inset:0;background:rgba(0,0,0,.65);display:grid;place-items:center;z-index:30}.modal.hidden{display:none}.modal-card{width:min(420px,92vw);background:#302218;border:1px solid rgba(255,255,255,.13);border-radius:8px;padding:24px;text-align:center}.modal-card p{color:#dbc5a1;line-height:1.6;margin-top:10px}@media(max-width:1120px){.ganh-shell{grid-template-columns:1fr;align-items:start}.panel,.board-wrap{width:min(680px,100%);margin:0 auto}.board{width:calc(100vw - 72px)}}@media(max-width:620px){.ganh-shell{padding:58px 10px 16px;gap:12px}.panel{padding:14px}.board-frame{padding:6px}.board{width:calc(100vw - 32px);border-width:8px}.point{width:50px;height:50px}.piece{border-width:3px}}
`;

export default function WebCoGanh(){
  const initialized=useRef(false); const gameApiRef=useRef<GameApi|null>(null); const roomRef=useRef(""); const playerColorRef=useRef<"w"|"b"|null>(null); const clientIdRef=useRef(""); const lastRemoteStampRef=useRef(""); const pushingRef=useRef(false); const pollTimerRef=useRef<number|null>(null);
  useEffect(()=>{const styleId="style-web_co_ganh"; if(!document.getElementById(styleId)){const style=document.createElement("style"); style.id=styleId; style.innerHTML=styles; document.head.appendChild(style)}
    const onlineStatus=document.getElementById("onlineStatus"); const createRoomBtn=document.getElementById("createRoomBtn") as HTMLButtonElement|null; const copyRoomBtn=document.getElementById("copyRoomBtn") as HTMLButtonElement|null; const joinRoomBtn=document.getElementById("joinRoomBtn") as HTMLButtonElement|null; const roomInput=document.getElementById("roomInput") as HTMLInputElement|null; const setOnlineStatus=(message:string)=>{if(onlineStatus) onlineStatus.textContent=message};
    const supabaseReady=Boolean(SUPABASE_URL&&SUPABASE_ANON_KEY); const headers={apikey:SUPABASE_ANON_KEY,Authorization:`Bearer ${SUPABASE_ANON_KEY}`,"Content-Type":"application/json"}; const apiUrl=(query="")=>`${SUPABASE_URL.replace(/\/$/,"")}/rest/v1/${ROOM_TABLE}${query}`; const roomLink=(roomId:string)=>`${window.location.origin}${window.location.pathname}?room=${roomId}`; const normalizeRoomId=(value:string)=>{try{const url=new URL(value); return url.searchParams.get("room")||value.trim()}catch{return value.trim()}}; const getClientId=()=>{const key="web_co_ganh_client_id"; const existing=window.localStorage.getItem(key); if(existing)return existing; const id=crypto.randomUUID(); window.localStorage.setItem(key,id); return id};
    const fetchRoom=async(roomId:string)=>{const res=await fetch(apiUrl(`?id=eq.${encodeURIComponent(roomId)}&select=*`),{headers,cache:"no-store"}); if(!res.ok) throw new Error(await res.text()); const data=await res.json(); return data?.[0]||null}; const patchRoom=async(roomId:string,payload:Record<string,unknown>)=>{const res=await fetch(apiUrl(`?id=eq.${encodeURIComponent(roomId)}`),{method:"PATCH",headers:{...headers,Prefer:"return=representation"},body:JSON.stringify({...payload,updated_at:new Date().toISOString()})}); if(!res.ok) throw new Error(await res.text()); const data=await res.json(); return data?.[0]||null};
    const pushState=async(state:unknown)=>{if(!roomRef.current||!supabaseReady||pushingRef.current)return; pushingRef.current=true; try{const row=await patchRoom(roomRef.current,{state}); if(row?.updated_at) lastRemoteStampRef.current=row.updated_at}catch(error){setOnlineStatus(`Không đẩy được nước đi: ${String(error).slice(0,120)}`)}finally{pushingRef.current=false}};
    const stopPolling=()=>{if(pollTimerRef.current) window.clearInterval(pollTimerRef.current); pollTimerRef.current=null}; const startPolling=(roomId:string)=>{stopPolling(); pollTimerRef.current=window.setInterval(async()=>{try{const room=await fetchRoom(roomId); if(!room)return; if(room.updated_at&&room.updated_at!==lastRemoteStampRef.current){lastRemoteStampRef.current=room.updated_at; gameApiRef.current?.loadState(room.state)}}catch(error){setOnlineStatus(`Mất kết nối phòng: ${String(error).slice(0,100)}`)}},1200)};
    const enterRoom=async(roomId:string)=>{if(!supabaseReady){setOnlineStatus("Chưa cấu hình Supabase env nên hiện chỉ chơi được 1 máy."); return} const room=await fetchRoom(roomId); if(!room){setOnlineStatus("Không tìm thấy phòng này."); return} const updates:Record<string,unknown>={}; if(room.white_id===clientIdRef.current) playerColorRef.current="w"; else if(room.black_id===clientIdRef.current) playerColorRef.current="b"; else if(!room.black_id){playerColorRef.current="b"; updates.black_id=clientIdRef.current}else playerColorRef.current=null; const nextRoom=Object.keys(updates).length?await patchRoom(roomId,updates):room; roomRef.current=roomId; lastRemoteStampRef.current=nextRoom?.updated_at||""; gameApiRef.current?.loadState(nextRoom?.state||room.state); if(copyRoomBtn) copyRoomBtn.disabled=false; if(roomInput) roomInput.value=roomId; window.history.replaceState(null,"",`${window.location.pathname}?room=${roomId}`); startPolling(roomId); const role=playerColorRef.current?`Bạn cầm quân ${playerColorRef.current==="w"?"Trắng":"Đen"}`:"Bạn đang xem ván"; setOnlineStatus(`Phòng ${roomId}. ${role}. Link đã sẵn sàng để copy.`)};
    const createRoom=async()=>{if(!supabaseReady){setOnlineStatus("Cần NEXT_PUBLIC_SUPABASE_URL và NEXT_PUBLIC_SUPABASE_ANON_KEY để tạo phòng online."); return} const roomId=Math.random().toString(36).slice(2,8).toUpperCase(); const res=await fetch(apiUrl(),{method:"POST",headers:{...headers,Prefer:"return=representation"},body:JSON.stringify({id:roomId,state:gameApiRef.current?.getState(),white_id:clientIdRef.current,black_id:null,updated_at:new Date().toISOString()})}); if(!res.ok){setOnlineStatus(`Không tạo được phòng: ${(await res.text()).slice(0,120)}`); return} roomRef.current=roomId; playerColorRef.current="w"; const data=await res.json(); lastRemoteStampRef.current=data?.[0]?.updated_at||""; if(copyRoomBtn) copyRoomBtn.disabled=false; if(roomInput) roomInput.value=roomId; window.history.replaceState(null,"",`${window.location.pathname}?room=${roomId}`); startPolling(roomId); setOnlineStatus(`Đã tạo phòng ${roomId}. Bạn cầm quân Trắng, gửi link cho người thứ hai.`)};
    const copyRoom=async()=>{if(!roomRef.current)return; await navigator.clipboard.writeText(roomLink(roomRef.current)); setOnlineStatus(`Đã copy link phòng ${roomRef.current}.`)};
    if(!initialized.current){initialized.current=true; clientIdRef.current=getClientId(); gameApiRef.current=initLogic({isOnline:()=>Boolean(roomRef.current),getPlayerColor:()=>playerColorRef.current,onStateChange:pushState})}
    const onCreateRoom=()=>void createRoom(); const onCopyRoom=()=>void copyRoom(); const onJoinRoom=()=>{const roomId=normalizeRoomId(roomInput?.value||""); if(roomId) void enterRoom(roomId)}; createRoomBtn?.addEventListener("click",onCreateRoom); copyRoomBtn?.addEventListener("click",onCopyRoom); joinRoomBtn?.addEventListener("click",onJoinRoom); const urlRoom=new URLSearchParams(window.location.search).get("room"); if(urlRoom){if(roomInput) roomInput.value=urlRoom; void enterRoom(urlRoom)}
    return()=>{stopPolling(); gameApiRef.current?.cleanup?.(); createRoomBtn?.removeEventListener("click",onCreateRoom); copyRoomBtn?.removeEventListener("click",onCopyRoom); joinRoomBtn?.removeEventListener("click",onJoinRoom); document.getElementById(styleId)?.remove()};
  },[]);
  return <div className="web_co_ganh-wrapper"><Link href="/GameMini" className="back-link">← GameMini</Link><div dangerouslySetInnerHTML={{__html:markup}} /></div>;
}
